import { DisciplinesFilterPipe } from './disciplines-filter.pipe';

describe('DisciplinesPipe', () => {
  it('create an instance', () => {
    const pipe = new DisciplinesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
